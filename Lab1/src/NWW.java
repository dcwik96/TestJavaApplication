
public class NWW {
    private NWD nwd = new NWD();

    public long nww(long a, long b) {
        if (a <= 0 || b <= 0) throw new IllegalArgumentException();

        return (a * b / nwd.nwd(a, b));
    }
}
